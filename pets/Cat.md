The Cat class:

extends Pet

inherits name, age, cleansed

adds iq

uses super() to initialize the parent fields

provides a getter for the IQ

So it represents a specific type of Pet with intelligence.
