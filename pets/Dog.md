The Dog class:

extends Pet

inherits name, age, cleansed

adds amountsOfCommand

uses super() to initialize the parent fields

provides a getter to access the command count

So it represents a specific type of pet that knows a certain number of commands.
